{
  "name": "@meterless/world-model-reference",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zero-dependency TypeScript reference implementation of the Meterless World Model contract (AGENTS.md). Runtime dependencies: none. devDependencies are tsx and vitest for running and testing, plus esbuild used only by scripts/build-browser-bundle.ts to emit the committed control-plane bundle.",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "idempotency-check": "tsx scripts/idempotency-check.ts",
    "build:browser": "tsx scripts/build-browser-bundle.ts"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "tsx": "^4.20.0",
    "vitest": "^3.2.0"
  }
}
